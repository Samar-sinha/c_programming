#include<stdio.h>
int main(){
    float m1 = 8; // maths marks
    float m2 = 8; // physics marks
    float m3 = 9; // english marks
    float m4 = 7; // chem marks
    float p = (m1 + m2 + m3 + m4) * 5/2;
    printf("percantage of 4 subjects : %f",p);  
    return 0;
}