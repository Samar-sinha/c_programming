#include<stdio.h>
#include<math.h>
int main(){
    // int a;
    // printf("Enter a number : ");
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("the square root of %d is %d",a,root);
    int p = pow(8,3);
    printf("%d",p);
    return 0;
}